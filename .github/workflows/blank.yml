name: DevFlow Worker

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  worker:
    runs-on: ubuntu-latest

    steps:
      - name: Call DevFlow API
        run: |
          curl -X GET https://devflow-ai-api.onrender.com
